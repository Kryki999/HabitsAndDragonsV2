# Mobile app agents

Repo-wide rules: [`../../AGENTS.md`](../../AGENTS.md).

This package is the V2 Expo shell. Keep domain folders (`habits`, `world`, `hero`, `social`, `mentor`). Do not import V1 `gameStore`, client LLM keys, or dragons-in-nav.

UI look (colors, components, screen archetypes, gate): [`../../docs/reference/ui/design-bible.md`](../../docs/reference/ui/design-bible.md). Look at `docs/reference/ui/golden/*.png` before building any screen. No colors/fonts/icon sets outside `docs/reference/ui/lookdev/tokens.css`.

Expo SDK docs for this app: https://docs.expo.dev/versions/v57.0.0/
